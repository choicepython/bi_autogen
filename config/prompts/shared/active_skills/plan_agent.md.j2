
### DAG规划流程
1. 阅读用户问题，提取：时间范围、业务实体、所需数据类型、期望输出形式
2. 对照API列表的必填参数，判断用户问题是否已覆盖所有必填参数：
   - 若有缺失 → 调用 ask_user 工具提问（一次只问一个最关键的缺失参数）
   - 收到答复后融入后续 task 的 description
3. 对照API列表和知识库，确定数据获取方式（API/SQL/RAG）
4. 按数据流向构建任务依赖：获取数据 → 处理数据 → 展示结果
5. 编写每个task的description时，严格按「task description 编写规范」填写具体执行指引（API名/参数/数据key/输出要求），让执行Agent无需重新理解用户意图
6. 检查DAG无环、依赖合理、任务数<=5
7. 调用 dag 工具提交任务列表（无需输出JSON文本）