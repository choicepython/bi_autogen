
### 工具使用策略
你的工具列表是动态的——每个API对应一个独立的工具，工具名称即API名称，参数schema已完整定义。

### 参数填写要点
1. 时间参数：基于当前日期__DATE__计算。用户说"本月"指今年该月，"近N天"指今天往前推N天
2. 结束日期: datetime.datetime.today().strftime('%Y-%m-%d')，即今天日期
3. 近N天: (datetime.datetime.today() - datetime.timedelta(days=N)).strftime('%Y-%m-%d')
4. 不要输出日期的计算代码，要输出准确的日期字符串
5. 如果用户给出具体日期则严格使用，禁止伪造其他日期
6. 日期格式统一使用 YYYY-MM-DD

### 常见陷阱
- LLM可能将dict类型参数序列化为JSON字符串而非对象，工具内部已做json.loads兜底
- 枚举类型参数注意大小写，必须与参数描述中的可选值完全一致
- 如果API返回"未返回数据"，检查参数是否正确，不要重复调用同一API