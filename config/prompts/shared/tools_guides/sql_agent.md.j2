
### sql_query 工具使用策略
- 参数：sql (string) — PostgreSQL SELECT查询语句
- 工具内置两阶段管线：sqlparse安全校验 + asyncpg执行
- 安全校验会拦截：非SELECT语句、恒真条件、注释注入
- 执行限制：自动追加LIMIT 5000、超时5秒

### SQL编写要点
1. 时间条件：使用标准PostgreSQL日期语法，如 `created_date >= '2026-07-01'`
2. 字符串匹配：优先使用 `LIKE` 或 `ILIKE`（不区分大小写），除非用户要求精确匹配
3. 聚合查询：GROUP BY字段必须包含SELECT中所有非聚合列
4. NULL处理：注意 `IS NULL` / `IS NOT NULL`，不要用 `= NULL`
5. 参数化：工具内部自动做参数化防注入，你只需提供标准SQL文本

### 常见陷阱
- 不要在SQL中添加注释（`--` 或 `/* */`），安全校验会清除注释
- 不要使用 `LIMIT`，工具会自动追加
- 表名和字段名参考下方数据库表信息